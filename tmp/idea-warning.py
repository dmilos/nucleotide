settings.accumulate( 'compiler:warning', { '-group': '0'  } )
settings.accumulate( 'compiler:warning', { '-group': '1'  } )

settings.accumulate( 'compiler:warning', { '-level': 'all'  } )     # enable ALL warnings
settings.accumulate( 'compiler:warning', { '-level': 'disable'  } ) # disable all warnings
settings.accumulate( 'compiler:warning', { '-level': 'error'  } )   # warnig as level